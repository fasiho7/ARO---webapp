import { career, milestone, practice, project, stage, t } from "../build";

export const fullstackDeveloper = career({
  id: "fullstack-developer",
  slug: "fullstack-developer",
  title: "Full-Stack Developer",
  category: "Development",
  icon: "layers",
  shortDescription:
    "Build both the interface and the API — JavaScript/TypeScript, React, Node/Express, and SQL — as one product.",
  description:
    "A full-stack developer ships a feature across the browser and the server: screens, HTTP, and a database. This path uses one language story — JavaScript and TypeScript on both sides — with React in the client, Express on the server, and PostgreSQL for data. HTML, CSS, and JavaScript come before React. HTTP and SQL come before you connect a UI to an API. You will go less deep on CSS than a frontend specialist and less deep on ops than a backend specialist. The goal is internships and junior full-stack roles, not a 30-day job guarantee.",
  whoItsFor: [
    "Students who want to own a feature from button click to database row.",
    "Beginners willing to practice programming, HTTP, and SQL — not only follow a 'MERN in a weekend' playlist.",
    "Career switchers aiming at full-stack internships or junior web roles in startups and services companies.",
  ],
  whatYouDo: [
    "Turn a product slice into a UI, an API, and tables that stay in sync.",
    "Structure pages, add enough CSS to be usable, and program the browser with JavaScript.",
    "Design REST endpoints, persist data in SQL, and handle loading, empty, and error states on both sides.",
    "Authenticate users end to end and deploy a stack someone else can click.",
    "Work with Git, READMEs, and honest scope so internships can review the work.",
  ],
  difficulty: "Intermediate",
  estimatedTime: "7–11 months",
  timeNote: "",
  prerequisites: [
    "Comfort using a computer, browser, and file system.",
    "Willingness to type code daily on both UI and server — watching videos is not enough.",
    "No prior programming required, but this path is longer than frontend-only or backend-only because you learn both.",
  ],
  skills: {
    core: [
      "HTML & CSS (working fluency)",
      "JavaScript",
      "TypeScript basics",
      "HTTP & REST",
      "SQL",
      "Node & Express",
      "React",
      "Git",
    ],
    technical: [
      "DOM and fetch",
      "Express routing",
      "PostgreSQL queries",
      "React state and routing",
      "Connecting UI to APIs",
      "Cookie or token auth (end to end)",
      "Testing both sides",
      "Full-stack deployment",
    ],
    tools: [
      "VS Code or Cursor",
      "Chrome DevTools",
      "Git & GitHub",
      "npm",
      "Node.js",
      "Express",
      "PostgreSQL",
      "React",
      "Vite",
    ],
    soft: [
      "Scoping a vertical slice instead of a fake-wide product",
      "Matching API contracts to UI states",
      "Writing READMEs that start both the API and the client",
      "Asking product questions before adding a sixth page",
    ],
    interview: [
      "Explain a request from click to SQL and back",
      "Live-code a small UI or a small Express route",
      "Walk through a full-stack project end to end",
      "Debug a CORS error, a 401, or a failed query with evidence",
    ],
  },
  tools: [
    "HTML",
    "CSS",
    "JavaScript",
    "TypeScript",
    "React",
    "Node.js",
    "Express",
    "PostgreSQL",
    "Git",
    "GitHub",
    "REST APIs",
    "Deployment",
  ],
  typicalRoles: [
    "Full-Stack Intern",
    "Junior Full-Stack Developer",
    "Junior Web Developer",
    "Software Engineer Intern (web)",
    "MERN/Node intern (title varies by company)",
  ],
  stages: [
    stage(
      0,
      "fs-fundamentals",
      "Computer & Web Fundamentals",
      "Learn files, the browser, and the client-server split before you pick React or Express.",
      "Full-stack bugs jump between laptop folders, the Network tab, and a server process. You need all three on a map.",
      [
        t("fs-fundamentals-computer", "Using a computer like a developer", "Create folders, edit text files, install a code editor, and keep client and server in sibling directories on purpose. Full-stack repos get messy faster than single-sided ones.", { time: "3–5 hours", skills: ["File system", "Editor setup"] }),
        t("fs-fundamentals-terminal", "Terminal enough to run two processes", "cd, run scripts, read stack traces, and stop a stuck port. You will often run a database, an API, and a Vite dev server at once.", { time: "3–5 hours", skills: ["Shell"] }),
        t("fs-fundamentals-browser", "How browsers work", "A browser requests a URL, receives HTML, then loads CSS, JavaScript, and later JSON from your API. DevTools Network is how you see which side failed.", { time: "3–4 hours", skills: ["HTTP requests", "DevTools Network"] }),
        t("fs-fundamentals-internet", "Internet & HTTP basics", "URLs, DNS, HTTP vs HTTPS, and status codes. Full-stack developers read failed requests from both the UI and the server logs.", { time: "4–6 hours", skills: ["URLs", "HTTP status codes"] }),
        t("fs-fundamentals-split", "What 'full stack' actually means", "UI, API, and data are three layers of one feature. This path will not make you a CSS specialist or a cloud architect — it will make you able to ship a slice.", { time: "2–3 hours", skills: ["System split"] }),
        t("fs-fundamentals-node-install", "Node.js and npm install", "Install Node, run node --version, and initialize a package.json. The same runtime will later serve Express and build the React app.", { time: "2–3 hours", skills: ["Node install", "npm"] }),
      ],
      {
        estimatedTime: "1–2 weeks",
        practice: [
          practice("task", "Open DevTools on any site, list HTML/CSS/JS files, then run node -e \"console.log('ok')\" in a terminal."),
        ],
        completionCriteria: [
          "Explain client vs server vs database in one feature.",
          "Run a Node one-liner and inspect a page's network requests.",
        ],
      },
    ),
    stage(
      1,
      "fs-html-css",
      "HTML & CSS (enough to ship UI)",
      "Semantic documents, forms, and layout that works on a phone. This is working fluency, not a frontend-specialist CSS career.",
      "A full-stack intern who cannot structure a form or a simple layout will stall on every feature. You do not need animation expertise.",
      [
        t("fs-html-document", "HTML document structure", "Doctype, head, body, and valid nesting. The API will not save a page that is a pile of unlabelled divs.", { time: "3–5 hours", skills: ["Document structure"] }),
        t("fs-html-semantic", "Semantic HTML & forms", "Landmarks, headings, labels, and input types. Full-stack features are usually forms talking to POST endpoints — unlabeled inputs become inaccessible and untestable.", { time: "5–7 hours", skills: ["Semantic HTML", "Forms"] }),
        t("fs-css-box", "Box model & cascade (working set)", "Content, padding, border, margin, and why width surprises happen. Learn enough cascade to stop fighting with !important.", { time: "4–6 hours", skills: ["Box model", "Selectors"] }),
        t("fs-css-layout", "Flexbox and simple Grid", "Nav bars, card rows, and a basic page shell. You are aiming at usable app layout, not marketing-site art direction.", { time: "6–8 hours", skills: ["Flexbox", "CSS Grid"] }),
        t("fs-css-responsive", "Responsive enough", "Viewport meta, fluid widths, and one or two breakpoints. Mobile-first internships in South Asia will open your app on a phone first.", { time: "4–5 hours", skills: ["Responsive layout"] }),
        t("fs-css-a11y", "Labels, contrast, and keyboard", "Every control has a label, contrast is readable, Tab order works. This is product quality, not a specialist track.", { time: "3–4 hours", skills: ["Basic a11y"] }),
      ],
      {
        estimatedTime: "2–3 weeks",
        projectIds: ["fs-proj-profile"],
        practice: [
          practice("project", "Build a multi-section page with a real form and a layout that works at 360px and 1280px — no UI library."),
        ],
        completionCriteria: [
          "Build a usable page without copying a tutorial.",
          "Every form control has a visible label.",
          "Layout holds together on a narrow phone width.",
        ],
      },
    ),
    stage(
      2,
      "fs-js",
      "JavaScript Fundamentals",
      "JavaScript is the language of this stack — browser and (later) Node. Master values, functions, and collections before React or Express.",
      "React and Express will not save you from weak JavaScript. Interviews and every handler start here.",
      [
        t("fs-js-values", "Variables & data types", "let, const, numbers, strings, booleans, null, undefined. Prefer const until mutation is required — shared objects cause UI and API bugs alike.", { time: "4–6 hours", skills: ["Variables", "Types"], codingHint: { track: "pf", topic: "variables" } }),
        t("fs-js-conditionals", "Operators & conditionals", "Comparison, logical operators, ===, if/else, and early returns. Truthiness bugs show up in both form validation and Express middleware.", { time: "4–5 hours", skills: ["Branching"], codingHint: { track: "pf", topic: "conditionals" } }),
        t("fs-js-loops", "Loops", "for, while, and for...of. Know when a loop is clearer than a clever one-liner — you will walk lists of records on both sides of the stack.", { time: "4–5 hours", skills: ["Loops"], codingHint: { track: "pf", topic: "loops" } }),
        t("fs-js-functions", "Functions", "Declarations, parameters, return values, and arrow functions as syntax — not as a personality. Functions are how you share validation between client and server later.", { time: "5–7 hours", skills: ["Functions"], codingHint: { track: "pf", topic: "functions" } }),
        t("fs-js-arrays", "Arrays", "map, filter, find, and why mutating arrays in place causes UI bugs. Many API responses are arrays you will transform in the client.", { time: "5–7 hours", skills: ["Arrays"], codingHint: { track: "dsa", topic: "arrays" } }),
        t("fs-js-objects", "Objects & JSON-shaped data", "Properties, nested objects, Object.keys. Treat objects as records — this is the shape of REST payloads.", { time: "5–6 hours", skills: ["Objects"] }),
        t("fs-js-dom", "DOM events & forms", "querySelector, addEventListener, preventDefault, and reading input values. You should collect form data without React before you collect it with React.", { time: "5–7 hours", skills: ["DOM", "Events"] }),
      ],
      {
        estimatedTime: "3–4 weeks",
        projectIds: ["fs-proj-todo-js"],
        practice: [
          practice("coding", "Solve small drills for functions and arrays, then wire the same logic to a form in the DOM.", { track: "pf" }),
        ],
        completionCriteria: [
          "Write functions that return values without global soup.",
          "Update the page in response to submit or click.",
          "Transform arrays with map/filter for a real UI list.",
        ],
      },
    ),
    stage(
      3,
      "fs-js-async",
      "Async JavaScript, Modules & TypeScript intro",
      "Promises, fetch, modules, and enough TypeScript to describe API payloads. This is still language work — not a framework.",
      "Connecting UI to APIs is mostly async and data shapes. Skipping this makes every Express tutorial feel like magic.",
      [
        t("fs-js-json", "JSON parse & stringify", "Invalid JSON is a data bug, not a CSS bug. You will log raw bodies more often than you think on both the client and the server.", { time: "2–3 hours", skills: ["JSON"] }),
        t("fs-js-modules", "ES modules", "import/export and splitting files. Both Vite and Node (with ESM or a bundler) reward small modules instead of one 2,000-line script.", { time: "3–4 hours", skills: ["ES modules"] }),
        t("fs-js-promises", "Promises & async/await", "Asynchronous work returns later. Error handling with try/catch around await, and why forgetting await is a classic bug on both sides.", { time: "6–8 hours", skills: ["async/await"], difficulty: "Intermediate" }),
        t("fs-js-fetch", "fetch in the browser", "fetch, Response.ok, json(), and showing an error the user can read. You are still calling other people's APIs — you will serve your own after SQL and Express.", { time: "4–6 hours", skills: ["fetch"] }),
        t("fs-js-ts-intro", "TypeScript as a data contract", "Types for objects you already use. You do not need to become a type theorist — you need to stop shipping typo bugs in User vs user payloads.", { time: "1–2 weeks", skills: ["TypeScript"], difficulty: "Intermediate" }),
        t("fs-js-node-script", "Node as a runtime (scripts)", "Run a .js/.ts script that reads a file or calls fetch from Node. See that the language is the same; the globals (window vs process) are not.", { time: "3–4 hours", skills: ["Node runtime"] }),
      ],
      {
        estimatedTime: "2–3 weeks",
        practice: [
          practice("project", "Fetch a public JSON API in the browser with loading and error states, using modules."),
        ],
        milestoneId: "fs-ms-1",
        completionCriteria: [
          "Fetch JSON and render it, including failure.",
          "Split code into modules.",
          "Type one payload without using any everywhere.",
        ],
      },
    ),
    stage(
      4,
      "fs-git",
      "Git & GitHub",
      "Version control is how you collaborate and how employers look at your work.",
      "A zip of MERN tutorials is not a portfolio. Full-stack repos especially need a README that starts two processes.",
      [
        t("fs-git-why", "Why version control", "Undo, branch, and collaborate without emailing zip files. Git is a tool for history, not a mysterious interview topic.", { time: "1–2 hours", skills: ["Version control"] }),
        t("fs-git-basics", "commit, status, log, diff", "Stage intentionally. Write commit messages that say why, not 'update'. Small commits make full-stack diffs reviewable.", { time: "4–6 hours", skills: ["git commit", "git diff"] }),
        t("fs-git-branch", "Branches & merge", "Feature branches that include UI and API changes together when they are one feature. Resolve a simple conflict so you are not afraid of Git.", { time: "4–5 hours", skills: ["Branches", "Merge"] }),
        t("fs-git-github", "GitHub remotes", "clone, push, pull, pull requests, and a public repo with a README. Employers open GitHub before they open your laptop.", { time: "3–5 hours", skills: ["GitHub", "Pull requests"] }),
        t("fs-git-ignore", ".gitignore & secrets", "Never commit node_modules, .env, or database dumps with passwords. This is a career-saving habit, not a nice-to-have.", { time: "1–2 hours", skills: [".gitignore"] }),
      ],
      {
        estimatedTime: "1 week",
        practice: [
          practice("task", "Put a project on GitHub with a README, .gitignore, and at least five meaningful commits."),
        ],
        completionCriteria: [
          "Create a repo, commit in small steps, and push to GitHub.",
          "Resolve a basic merge conflict in a practice repo.",
        ],
      },
    ),
    stage(
      5,
      "fs-http",
      "HTTP & REST (before you connect a UI to your API)",
      "Methods, status codes, headers, and JSON as a contract. Practice with curl before Express generates it for you.",
      "If HTTP is fuzzy, CORS, 401s, and 'it works in Postman' will eat your internship.",
      [
        t("fs-http-model", "Requests and responses", "Method, URL, headers, body in; status, headers, body out. The React app and Express app are two sides of this conversation.", { time: "3–4 hours", skills: ["HTTP model"] }),
        t("fs-http-methods", "GET, POST, PUT, PATCH, DELETE", "Safe vs unsafe methods and why GET should not create rows. Your UI's buttons must match the method the API expects.", { time: "4–6 hours", skills: ["HTTP methods"] }),
        t("fs-http-status", "Status codes for UI states", "2xx, 4xx, 5xx. The frontend should branch on Response.ok and specific codes — not on whether the JSON 'looks empty'.", { time: "3–4 hours", skills: ["Status codes"] }),
        t("fs-http-headers", "Headers, JSON, and CORS as a concept", "Content-Type and Authorization. CORS is a browser rule you will hit the first time a Vite app calls localhost:3001 — understand it before you copy a * origin.", { time: "4–5 hours", skills: ["Headers", "CORS"] }),
        t("fs-http-rest", "Resources and query params", "Nouns in URLs, filters in the query string, bodies for writes. Design the contract on paper; both sides will implement it.", { time: "4–5 hours", skills: ["REST"] }),
        t("fs-http-tools", "curl or Postman", "Reproduce any UI request outside the browser. If you cannot, you cannot tell which side is broken.", { time: "3–4 hours", skills: ["curl"] }),
      ],
      {
        estimatedTime: "1–2 weeks",
        practice: [
          practice("exercise", "Call a public API with curl, then the same URL with fetch. Match status codes and JSON."),
        ],
        completionCriteria: [
          "Explain GET vs POST and CORS as a browser policy, not as 'auth'.",
          "Write down a resource list (URLs + methods) for a tiny product.",
        ],
      },
    ),
    stage(
      6,
      "fs-sql",
      "SQL & Relational Data (before connecting UI + API)",
      "Tables, joins, and constraints. The database is a third runtime — not an afterthought of React state.",
      "Full-stack bugs are often data bugs. If you cannot query it, the UI cannot honestly show it.",
      [
        t("fs-sql-relational", "Tables, rows, and keys", "Primary keys, foreign keys, and why duplicating the user in every row will haunt you. Sketch schema before Express models.", { time: "4–6 hours", skills: ["Schema", "Keys"] }),
        t("fs-sql-select", "SELECT, WHERE, ORDER, LIMIT", "Filtering and paging in SQL. Loading the whole table into Node then filtering is a trap you will feel at intern-scale data.", { time: "5–7 hours", skills: ["SELECT"] }),
        t("fs-sql-join", "JOINs", "INNER vs LEFT JOIN. A UI that shows 'task with project name' is a join, not two unrelated fetches you hope stay consistent.", { time: "6–8 hours", skills: ["JOINs"], difficulty: "Intermediate" }),
        t("fs-sql-write", "INSERT, UPDATE, DELETE", "Write paths and why DELETE needs WHERE. Practice on a local Postgres or SQLite until the statements are boring.", { time: "4–5 hours", skills: ["DML"] }),
        t("fs-sql-constraints", "Constraints & transactions (intro)", "UNIQUE, NOT NULL, and 'all of this or none of it' for register-plus-profile. Constraints are cheaper than UI validation alone.", { time: "4–6 hours", skills: ["Constraints", "Transactions"], difficulty: "Intermediate" }),
        t("fs-sql-postgres", "PostgreSQL locally", "Create a database, run a .sql file, and connect with a GUI or psql. SQLite is acceptable while learning, but name Postgres as the production-shaped default for this path.", { time: "3–5 hours", skills: ["PostgreSQL"] }),
      ],
      {
        estimatedTime: "2–3 weeks",
        projectIds: ["fs-proj-sql-schema"],
        practice: [
          practice("lab", "Design tables for a campus club or shop and write the joins your future screens would need."),
        ],
        milestoneId: "fs-ms-2",
        completionCriteria: [
          "Write joins and filters without a cheat sheet for every query.",
          "Explain keys and constraints on a schema you drew.",
        ],
      },
    ),
    stage(
      7,
      "fs-node",
      "Node.js & Express Fundamentals",
      "Build HTTP APIs in JavaScript/TypeScript. In-memory or file storage is fine until the next stage wires SQL.",
      "You now know HTTP. Express is how this path implements it — not a substitute for understanding status codes.",
      [
        t("fs-node-modules", "Node project layout", "package.json scripts, src folder, and nodemon or equivalent. Keep the API a separate process from the Vite app even in a monorepo.", { time: "3–4 hours", skills: ["Node projects"] }),
        t("fs-express-routes", "Express routing", "app.get/post, route params, and sending JSON. Routes should map to the resources you already designed on paper.", { time: "5–7 hours", skills: ["Express routing"] }),
        t("fs-express-middleware", "Middleware", "Logging, JSON body parser, and error middleware. Middleware is how you will later add auth without copy-pasting into every route.", { time: "4–6 hours", skills: ["Middleware"], difficulty: "Intermediate" }),
        t("fs-express-validation", "Validate request bodies", "Check required fields and types; return 400 with a stable shape. Shared TypeScript types help; they do not replace runtime checks at the edge.", { time: "4–5 hours", skills: ["Validation"] }),
        t("fs-express-errors", "Error handling & status codes", "404 vs 400 vs 500. Never send a raw stack trace to the browser in a 'production' mindset, even in portfolio apps.", { time: "3–4 hours", skills: ["Error handling"] }),
        t("fs-express-structure", "Routers & services", "Thin route handlers that call functions. This is the same idea as backend layering, kept smaller than a specialist backend path.", { time: "4–5 hours", skills: ["Layering"], difficulty: "Intermediate" }),
      ],
      {
        estimatedTime: "2–3 weeks",
        projectIds: ["fs-proj-notes-api"],
        practice: [
          practice("project", "Build a CRUD Express API and prove it with curl before any React UI exists."),
        ],
        completionCriteria: [
          "Expose list/create/get/update/delete with correct status codes.",
          "Validate input and return useful 400s.",
          "Keep the API runnable with npm start and a README.",
        ],
      },
    ),
    stage(
      8,
      "fs-express-db",
      "PostgreSQL in the Node App",
      "Persist Express resources with SQL, migrations, and env-based connection strings.",
      "A JSON file is a learning step. Internships expect data that survives restart.",
      [
        t("fs-db-connect", "Connection strings & env", "pg (or a small query helper), DATABASE_URL, and never committing passwords. One wrong URL is a week of 'works on my machine'.", { time: "3–4 hours", skills: ["pg", "Env vars"] }),
        t("fs-db-queries", "Parameterized queries", "Never concat SQL with user strings. $1-style parameters are a security and correctness habit.", { time: "4–6 hours", skills: ["Parameterized SQL"] }),
        t("fs-db-migrations", "Migrations", "Schema as versioned files. Do not drop the database to add a column when a teammate already has data.", { time: "4–6 hours", skills: ["Migrations"], difficulty: "Intermediate" }),
        t("fs-db-crud", "CRUD against tables", "Each resource maps to queries you can print. If you use an ORM, still read the SQL it emits.", { time: "5–7 hours", skills: ["SQL in Node"] }),
        t("fs-db-seed", "Seed scripts", "Sample users and rows so the UI has something to render. Empty databases make terrible demos and worse interviews.", { time: "2–3 hours", skills: ["Seed data"] }),
        t("fs-db-nplusone", "Avoid SELECT * then filter in JS", "Filter, join, and paginate in SQL. This is the full-stack version of the backend N+1 lesson, kept practical.", { time: "3–4 hours", skills: ["Query design"], difficulty: "Intermediate" }),
      ],
      {
        estimatedTime: "2 weeks",
        projectIds: ["fs-proj-db-api"],
        practice: [
          practice("project", "Move your Express CRUD onto PostgreSQL with a migration and a seed script."),
        ],
        completionCriteria: [
          "Restart the API and still see the data.",
          "Show parameterized SQL for list and create.",
        ],
      },
    ),
    stage(
      9,
      "fs-react",
      "React Enough to Build Screens",
      "Components, state, effects, and routing. React comes after HTML/CSS/JS — it is not a replacement for them.",
      "You are ready for React only if you can already collect form data and fetch JSON. Otherwise you will copy JSX without understanding renders.",
      [
        t("fs-react-components", "Components, JSX, and props", "UI as a tree. Props in, UI out. This idea matters more than the file generator in your starter.", { time: "5–7 hours", skills: ["Components", "Props"] }),
        t("fs-react-state", "useState & forms", "Controlled inputs, submit handlers, and lifting state when two components need the same data. Forms are how most intern features start.", { time: "5–7 hours", skills: ["useState", "Forms"] }),
        t("fs-react-lists", "Lists & keys", "Keys identify items across renders. Index keys break when lists reorder — your API will reorder.", { time: "3–4 hours", skills: ["Keys"] }),
        t("fs-react-effects", "useEffect for fetching (carefully)", "Synchronize with the outside world. Effects are not glue for everything, and stale responses must be ignored.", { time: "6–8 hours", skills: ["useEffect"], difficulty: "Intermediate" }),
        t("fs-react-router", "Client-side routing", "Multiple screens, URL as state, back button. Filters that belong in the query string should live there even before you add a global store.", { time: "4–6 hours", skills: ["Routing"] }),
        t("fs-react-ts", "TypeScript props", "Type the data you already fetch. any everywhere is how full-stack payload bugs hide until production.", { time: "4–6 hours", skills: ["TypeScript", "Props types"], difficulty: "Intermediate" }),
      ],
      {
        estimatedTime: "3 weeks",
        projectIds: ["fs-proj-react-ui"],
        practice: [
          practice("project", "Build a multi-screen React app against a public or mocked API with loading and error states."),
        ],
        completionCriteria: [
          "Split UI into components with props and state.",
          "Fetch in an effect and handle empty/error.",
          "Navigate between screens without losing the back button.",
        ],
      },
    ),
    stage(
      10,
      "fs-connect",
      "Connecting Frontend and Backend",
      "Point React at your Express API: shared contracts, CORS, env URLs, and UI states that match status codes.",
      "This is the actual full-stack skill. Until now you had two half-products.",
      [
        t("fs-connect-cors", "CORS and dev URLs", "Allow the Vite origin in Express, or proxy. Understand why the browser blocks, not only which Stack Overflow snippet to paste.", { time: "3–4 hours", skills: ["CORS"] }),
        t("fs-connect-env", "API base URLs", "VITE_API_URL (or equivalent) for dev vs prod. Hardcoding localhost in a deployed frontend is a classic intern outage.", { time: "2–3 hours", skills: ["Env vars"] }),
        t("fs-connect-states", "Loading, empty, error, success", "Map HTTP results to UI. A spinner forever is a missing timeout and error path on one of the two sides.", { time: "4–5 hours", skills: ["UI states"] }),
        t("fs-connect-contract", "Keep the contract in one place", "Shared types or a documented OpenAPI/README table. When the API field name changes, the UI should fail obviously.", { time: "3–4 hours", skills: ["API contracts"], difficulty: "Intermediate" }),
        t("fs-connect-crud-ui", "CRUD screens against your API", "List, create, edit, delete with honest failure. Confirm deletes; do not trust the UI if the DELETE failed.", { time: "1–2 weeks", skills: ["Full-stack CRUD"], difficulty: "Intermediate" }),
        t("fs-connect-debug", "Which side is broken?", "Network tab vs server log vs SQL. Full-stack debugging is a procedure, not a vibe.", { time: "3–4 hours", skills: ["Debugging"] }),
      ],
      {
        estimatedTime: "2 weeks",
        projectIds: ["fs-proj-connected"],
        practice: [
          practice("project", "Connect a React UI to your Express+SQL API. Demo create and list with the Network tab open."),
        ],
        milestoneId: "fs-ms-3",
        completionCriteria: [
          "A click in the UI creates a row you can see in SQL.",
          "Explain a failure using status code + log line.",
          "Dev and the idea of a production API URL are not the same string.",
        ],
      },
    ),
    stage(
      11,
      "fs-auth",
      "Full-Stack Authentication",
      "Register, login, hashed passwords, and a UI that only shows what the session may do.",
      "A public CRUD app is homework. End-to-end auth is what junior full-stack interviews poke.",
      [
        t("fs-auth-passwords", "Hash passwords on the server", "bcrypt (or similar) in Express. The client must never 'hash instead of HTTPS' as a security story — HTTPS plus server hashing.", { time: "4–5 hours", skills: ["Password hashing"] }),
        t("fs-auth-session", "Sessions or JWT — pick one and finish it", "HTTP-only cookies or Authorization headers. Know the tradeoff; implement one path completely, including expiry.", { time: "6–8 hours", skills: ["Sessions", "JWT"], difficulty: "Intermediate" }),
        t("fs-auth-protect", "Protect API routes", "Reject missing/invalid credentials with 401; reject the wrong user with 403. The UI cannot be the only gate.", { time: "4–6 hours", skills: ["Auth middleware"] }),
        t("fs-auth-ui", "Login, logout, and gated screens", "Redirect unauthenticated users, show the current user, and clear state on logout. Do not store passwords in localStorage.", { time: "5–7 hours", skills: ["Auth UI"] }),
        t("fs-auth-cors-cookies", "Credentials and CORS", "If you use cookies, credentials: 'include' and a specific origin — not *. This is where copy-paste CORS dies.", { time: "3–4 hours", skills: ["Cookie CORS"], difficulty: "Intermediate" }),
      ],
      {
        estimatedTime: "2 weeks",
        projectIds: ["fs-proj-auth-app"],
        practice: [
          practice("project", "Add register/login to a connected app. Prove with curl that writes require auth, then prove the UI respects it."),
        ],
        completionCriteria: [
          "Passwords stored as hashes.",
          "Unauthorized API writes fail even if you bypass the UI.",
          "Logout actually ends the session from the user's point of view.",
        ],
      },
    ),
    stage(
      12,
      "fs-testing",
      "Testing Both Sides",
      "Unit-test logic, hit Express with a test client, and test one critical React interaction.",
      "You do not need 100% coverage. You need tests that fail when auth or checkout-style flows break.",
      [
        t("fs-test-why", "What to test in a full-stack app", "Validators, permission checks, status codes, and one user-visible flow. Skip snapshotting all CSS — it will not catch a 401 hole.", { time: "2–3 hours", skills: ["Test strategy"] }),
        t("fs-test-api", "API tests", "Create a user, reject a bad token, assert JSON. Faster than clicking and more honest than console.log.", { time: "5–7 hours", skills: ["API tests"], difficulty: "Intermediate" }),
        t("fs-test-db", "Tests against a disposable database", "A test database or rolled-back transactions. Sharing your dev DB with tests will lie to you.", { time: "4–5 hours", skills: ["DB tests"], difficulty: "Intermediate" }),
        t("fs-test-ui", "Component or flow tests", "Render a form, submit, assert error text. Query by role so tests resemble accessibility.", { time: "4–6 hours", skills: ["UI tests"] }),
        t("fs-test-contract", "A test that locks the contract", "If the API renames a field, a test should fail on one side. That failure is the point of having a contract.", { time: "3–4 hours", skills: ["Contract tests"] }),
      ],
      {
        estimatedTime: "1–2 weeks",
        practice: [
          practice("exercise", "Add API tests for auth and one UI test for a validation message."),
        ],
        completionCriteria: [
          "A sabotaged auth check makes a test fail.",
          "You can run tests with one documented command.",
        ],
      },
    ),
    stage(
      13,
      "fs-deploy",
      "Deploying a Full-Stack App",
      "Public UI, public API, hosted Postgres, env vars, and HTTPS. Localhost is not a demo.",
      "Employers click the live link. A screen recording of localhost is a backup, not the bar.",
      [
        t("fs-deploy-build", "Production builds", "Vite build vs Express start, NODE_ENV, and failing the build on type errors if you use TypeScript. Debug defaults do not belong in production.", { time: "3–4 hours", skills: ["Production build"] }),
        t("fs-deploy-split", "Two services or a combined host", "Static frontend plus API process, or a host that serves both. Document which you chose and why so a reviewer is not guessing ports.", { time: "4–6 hours", skills: ["Architecture"] }),
        t("fs-deploy-db", "Hosted Postgres & migrations", "Run migrations against the cloud database. Seed only what is safe; never seed production with fake admin passwords you committed.", { time: "4–6 hours", skills: ["Hosted SQL"], difficulty: "Intermediate" }),
        t("fs-deploy-env", "Env vars in the host", "API URL in the frontend build, database URL on the server, secrets not in git. Rebuild the frontend when the API URL changes.", { time: "3–4 hours", skills: ["Env vars"] }),
        t("fs-deploy-https", "HTTPS and a live checklist", "Both origins HTTPS, cookies if any still work, CORS origins updated. Click from a phone on another network.", { time: "3–5 hours", skills: ["HTTPS"] }),
      ],
      {
        estimatedTime: "1–2 weeks",
        practice: [
          practice("task", "Deploy UI + API + database. Put both URLs and a test account (or seed instructions) in the README."),
        ],
        milestoneId: "fs-ms-4",
        completionCriteria: [
          "A stranger can open the live UI and complete one flow.",
          "You can explain how env vars differ between laptop and host.",
        ],
      },
    ),
    stage(
      14,
      "fs-projects",
      "Real-World Full-Stack Projects",
      "Vertical slices that look like products: permissions, empty states, and a README that starts the whole stack.",
      "Depth beats ten unfinished MERN clones. One shoppable workflow beats a landing page plus a leftover API.",
      [
        t("fs-projects-scope", "Scope a vertical slice", "Users, screens, endpoints, tables, non-goals. Full-stack unfinishedness usually means the UI and API drifted.", { time: "3–4 hours", skills: ["Product scoping"] }),
        t("fs-projects-quality", "Production-style quality", "Validation on both sides, pagination, and empty states. Pretty CSS cannot hide a 500 on submit.", { time: "ongoing", skills: ["Polish"] }),
        t("fs-projects-roles", "Two roles in one product", "Owner vs member, or applicant vs poster. Permissions must hold if the UI is stripped.", { time: "1–2 weeks", skills: ["Authorization"], difficulty: "Advanced" }),
        t("fs-projects-files", "Uploads or attachments (optional but strong)", "Store files off-DB (object storage or a local folder with care). Know what you would not store in Postgres.", { time: "1 week", skills: ["File uploads"], difficulty: "Advanced" }),
        t("fs-projects-readme", "Monorepo or two-folder README", "How to run DB, API, and UI; how to migrate; which ports. If a reviewer cannot start it, it is unfinished.", { time: "3–5 hours", skills: ["Documentation"] }),
      ],
      {
        estimatedTime: "4–8 weeks",
        projectIds: ["fs-proj-saas", "fs-proj-marketplace"],
        practice: [
          practice("project", "Finish two substantial full-stack apps to a demoable standard instead of starting five."),
        ],
        milestoneId: "fs-ms-5",
        completionCriteria: [
          "Two apps with live URLs, source, and a permission rule you can demo.",
          "Each README starts the whole stack.",
        ],
      },
    ),
    stage(
      15,
      "fs-interview",
      "Portfolio & Interview Preparation",
      "Make a hiring manager understand a click-to-row story in five minutes, then practice talking through it.",
      "Silent GitHub profiles lose to weaker code with a clear end-to-end story.",
      [
        t("fs-int-portfolio", "Portfolio site", "Link live demos, source, and a short architecture note per project (UI, API, schema). The portfolio can be simpler CSS than a frontend specialist's.", { time: "1–2 weeks", skills: ["Portfolio"] }),
        t("fs-int-github", "GitHub hygiene", "Pinned repos, env examples, no node_modules. Commits should look like a person who finished a slice.", { time: "3–5 hours", skills: ["GitHub profile"] }),
        t("fs-int-resume", "Resume for full-stack", "Impact bullets that mention both sides: built X with React + Express + Postgres. One page if early career.", { time: "4–6 hours", skills: ["Resume"] }),
        t("fs-int-js", "JavaScript & React questions", "Closures, async, state vs props — explained while you sketch. Honesty about CSS depth is better than fake specialist claims.", { time: "1–2 weeks", skills: ["JS interviews", "React interviews"], difficulty: "Intermediate" }),
        t("fs-int-http-sql", "HTTP, Express, and SQL questions", "Status codes, joins, parameterized queries, 401 vs 403. Be ready to write a route and a SELECT on paper.", { time: "1–2 weeks", skills: ["API interviews", "SQL interviews"] }),
        t("fs-int-dsa-light", "Light problem solving", "Enough array/string practice to not freeze. Some intern loops include DSA; many include a small feature across UI+API.", { time: "1–2 weeks", skills: ["Problem solving"], kind: "coding", codingHint: { track: "dsa", topic: "arrays" } }),
        t("fs-int-walkthrough", "The end-to-end walkthrough", "Narrate one feature: table, route, fetch, component. This is the full-stack interview more than a library trivia quiz.", { time: "1 week", skills: ["Communication"] }),
        t("fs-int-mock", "Mock interviews", "One timed UI or route prompt and one project walkthrough. Record yourself once — it is uncomfortable and useful.", { time: "1 week", skills: ["Mock interviews"] }),
      ],
      {
        estimatedTime: "3–5 weeks",
        projectIds: ["fs-proj-portfolio-site", "fs-proj-case-study"],
        practice: [
          practice("task", "Do one timed prompt and one 8-minute click-to-SQL walkthrough out loud."),
        ],
        completionCriteria: [
          "Walk through a feature without opening chat for the code.",
          "Answer 401 vs 403 and show it in a project.",
        ],
      },
    ),
    stage(
      16,
      "fs-job-ready",
      "Job Ready",
      "You are aiming at internships and junior full-stack or web roles, not at knowing every library in the JS ecosystem.",
      "Readiness is a demoable slice plus the ability to debug both sides.",
      [
        t("fs-job-checklist", "Career-specific proof", "You can ship a feature across UI, API, and SQL. You know you are not a CSS or Kubernetes specialist yet — and you can say that without apologizing.", { time: "3–5 days", skills: ["Self-assessment"] }),
        t("fs-job-apply", "Applications with a system", "Track roles, tailor two sentences, and keep live links ready. Spray-and-pray without a demo wastes weeks.", { time: "ongoing", skills: ["Job search process"] }),
        t("fs-job-south-asia", "Intern and junior markets", "Campus internships, service-company junior web roles, and startup generalists are realistic. 'Full-stack ninja' titles are not a 7-month promise.", { time: "2–3 hours", skills: ["Market expectations"] }),
        t("fs-job-first-week", "What juniors actually do", "Tickets that touch one extra file on the other side of the stack, bugs, and asking for help with Network + logs. The job keeps teaching.", { time: "2–3 hours", skills: ["Junior expectations"] }),
        t("fs-job-learn-next", "What you will learn at work", "Deeper CSS or deeper backend, their cloud, and code review. Naming that in interviews is a strength.", { time: "2–3 hours", skills: ["Growth plan"] }),
      ],
      {
        estimatedTime: "2–4 weeks (alongside applications)",
        milestoneId: "fs-ms-6",
        practice: [
          practice("task", "Complete the job-ready checklist with evidence (URLs, repos, and notes), not just ticks."),
        ],
        completionCriteria: [
          "Every item on the job-ready checklist has an artifact or an honest 'not yet'.",
          "You can name two roles you are actually qualified to apply for.",
        ],
      },
    ),
  ],
  projects: [
    project("fs-proj-profile", "Personal profile + contact form", "beginner", "Semantic HTML, a labelled form, and layout that works on a phone. No React yet — prove you can structure a page.", { stageId: "fs-html-css", skills: ["HTML", "CSS", "Forms"], expectedOutcome: "A usable page with CSS disabled still making sense structurally." }),
    project("fs-proj-todo-js", "DOM todo or notes app", "beginner", "Add, complete, filter, persist in localStorage. JavaScript state in the browser before React. Practice fetch separately with a public JSON API before you serve your own.", { stageId: "fs-js", skills: ["JavaScript", "DOM"] }),
    project("fs-proj-sql-schema", "Relational schema lab", "beginner", "Tables for a club or shop, seed data, and the joins your future screens need. SQL before Express.", { stageId: "fs-sql", skills: ["SQL", "PostgreSQL"] }),
    project("fs-proj-notes-api", "Express notes API", "intermediate", "CRUD with validation and status codes, proven with curl. In-memory is acceptable if HTTP design is clean.", { stageId: "fs-node", skills: ["Express", "REST"] }),
    project("fs-proj-db-api", "Persisted resource API", "intermediate", "The notes (or issues) API on PostgreSQL with migrations and seed data.", { stageId: "fs-express-db", skills: ["Express", "SQL"] }),
    project("fs-proj-react-ui", "Multi-screen React client", "intermediate", "App shell, list/detail, filters, loading/error. Can still talk to a public API until the connect stage.", { stageId: "fs-react", skills: ["React", "Routing"] }),
    project("fs-proj-connected", "Connected CRUD product", "intermediate", "React + Express + SQL on one domain: create in the UI, see the row in the database.", { stageId: "fs-connect", skills: ["React", "Express", "SQL"] }),
    project("fs-proj-auth-app", "Authenticated full-stack app", "advanced", "Register/login, hashed passwords, gated UI, and API writes that fail without a session even if you bypass the UI.", { stageId: "fs-auth", skills: ["Auth", "React", "Express"], expectedOutcome: "You can demo 401 via curl and a logged-in happy path in the browser." }),
    project("fs-proj-saas", "SaaS-style intern tool", "advanced", "App shell, settings, and a core workflow (tickets, CRM-lite, or campus society admin) with two roles.", { stageId: "fs-projects", skills: ["Full-stack", "RBAC"], expectedOutcome: "Feels like an internal tool, not a tutorial clone." }),
    project("fs-proj-marketplace", "Marketplace or booking app", "advanced", "Listings plus a request/order flow, constraints in SQL, and empty states. Pagination on list endpoints.", { stageId: "fs-projects", skills: ["Domain modeling", "SQL", "React"] }),
    project("fs-proj-portfolio-site", "Polished portfolio", "portfolio", "Three strongest projects, live links, and a click-to-SQL writeup on at least one. The site itself can be simple and fast.", { stageId: "fs-interview", skills: ["Portfolio", "Copy"], expectedOutcome: "A hiring manager understands your strongest full-stack work in five minutes." }),
    project("fs-proj-case-study", "End-to-end case study", "portfolio", "Written architecture of one advanced app: schema, routes, auth, and what you would do with more time.", { stageId: "fs-interview", skills: ["Communication", "Tradeoffs"] }),
  ],
  milestones: [
    milestone(1, "fs-ms-1", "Web & JavaScript Foundations", "You can structure a page and program the browser, including async fetch.", ["fs-fundamentals", "fs-html-css", "fs-js", "fs-js-async"], ["Build a usable page without copying a tutorial.", "Fetch JSON and render it with error handling."]),
    milestone(2, "fs-ms-2", "HTTP & SQL Foundations", "You can design a contract and query a database before wiring your own stack.", ["fs-git", "fs-http", "fs-sql"], ["Explain HTTP methods and status codes.", "Write joins on a schema you designed.", "Keep work on GitHub."]),
    milestone(3, "fs-ms-3", "Connected Full-Stack Developer", "You can implement Express+SQL and React, then connect them.", ["fs-node", "fs-express-db", "fs-react", "fs-connect"], ["A UI action persists a database row.", "You can debug which side failed."]),
    milestone(4, "fs-ms-4", "Production-minded Full-Stack", "You care about auth, tests, and a live deploy of the whole stack.", ["fs-auth", "fs-testing", "fs-deploy"], ["Protect writes on the API, not only hide buttons.", "Ship a live URL that completes a flow."]),
    milestone(5, "fs-ms-5", "Portfolio Ready", "You have substantial products that look like work.", ["fs-projects"], ["Two or more polished full-stack apps with READMEs and demos."]),
    milestone(6, "fs-ms-6", "Job Ready", "You can interview without pretending to be a CSS or cloud specialist.", ["fs-interview", "fs-job-ready"], ["Walk through click-to-SQL.", "Job-ready checklist has evidence."]),
  ],
  interviewPrep: {
    summary:
      "Full-stack intern interviews mix JavaScript, a small UI or Express route, SQL, and a project deep-dive from click to row. Some add light DSA; few need Kubernetes on day one.",
    technical: [
      "JavaScript: functions, async/await, array methods",
      "React: state, props, effects, keys — working depth, not specialist trivia",
      "HTTP methods, status codes, CORS vs auth",
      "Express routing, validation, parameterized SQL",
      "Auth: hashing, 401 vs 403, cookies vs tokens at a practical level",
    ],
    problemSolving: [
      "Small array/string exercises so you do not freeze",
      "Live-code a form plus POST handler, or a SELECT plus a list UI",
      "Debug CORS, a 401, or a failed query with Network tab and logs",
    ],
    projects: [
      "Narrate one feature across table, route, and component",
      "Show a permission that still holds if the UI is bypassed",
      "Be honest about CSS depth and what you have not deployed",
    ],
    behavioral: [
      "A time UI and API contracts drifted and how you fixed them",
      "A bug you located by asking which layer failed first",
      "How you ask for help with expected vs actual on both sides",
    ],
    portfolio: [
      "Live UI + working API, not only GitHub source",
      "READMEs that start database, server, and client",
      "A one-page resume with those links",
    ],
  },
  jobReadiness: [
    "HTML/CSS/JS completed with projects before React — not only tutorials",
    "SQL schema you can explain, plus Express CRUD with parameterized queries",
    "At least one React UI that talks to your API with loading/error/empty states",
    "End-to-end auth with hashed passwords and protected routes",
    "Tests on a risky API path and a documented way to run them",
    "GitHub with 2–4 polished full-stack projects, live URLs, and READMEs",
    "No secrets in the repo; env examples documented",
    "Interview practice: JS/React/HTTP/SQL plus one click-to-SQL walkthrough",
    "You can explain what you would learn next (deeper CSS, deeper backend, or their cloud)",
  ],
  outcomes: [
    "Full-stack intern or junior full-stack / web developer roles",
    "Junior software engineer roles on product teams that want generalists",
    "A foundation to later specialize in frontend or backend without starting over",
    "Ability to ship a vertical slice, not only complete separate UI and API tutorials",
  ],
});
